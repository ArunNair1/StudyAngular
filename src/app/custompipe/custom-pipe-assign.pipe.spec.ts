import { CustomPipeAssignPipe } from './custom-pipe-assign.pipe';

describe('CustomPipeAssignPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipeAssignPipe();
    expect(pipe).toBeTruthy();
  });
});
